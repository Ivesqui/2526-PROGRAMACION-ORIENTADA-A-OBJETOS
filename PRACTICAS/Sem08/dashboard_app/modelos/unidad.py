class Unidad:
    def __init__(self, nombre, ruta):
        # Nombre visible de la unidad (carpeta)
        self.nombre = nombre

        # Ruta completa donde está ubicada la unidad
        self.ruta = ruta
